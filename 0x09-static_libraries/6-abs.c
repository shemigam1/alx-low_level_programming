#include "main.h"

/**
 * _abs - a function that prints the absolute value of an int
 * @n: variable
 * Return: 0
 */

int _abs(int n)
{
	if (n < 0)
	{
		return (-n);
	}
	else
	{
		return (n);
	}
	return (0);
}
